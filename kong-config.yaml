_format_version: "3.0"

services:
  - name: mood-tracker
    url: http://mood-tracker-service:8080

routes:
  - name: mood-route
    service: mood-tracker
    paths:
      - /mood
    strip_path: true
